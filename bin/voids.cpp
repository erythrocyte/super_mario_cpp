#include "voids.hpp"

#include <string>

// #include <SFML/Graphics.hpp>

// #include "gui/frame.hpp"

// namespace g = sm::src::gui;

namespace sm::bin {
void run()
{
    std::string a = "Tests";
    // auto frame = std::make_shared<g::Frame>(200, 200, a);

    // auto circle = std::make_shared<sf::CircleShape>(100.f);
    // circle->setFillColor(sf::Color::Green);

    // frame->add_drawable(circle);

    // frame->run();
}
}